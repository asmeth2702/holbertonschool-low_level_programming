#include "main.h"
/**
 * add - addition operation.
 * @a: numbers to be added.
 * @b: numbers to be addad.
 * Return - @n: tha sum of two numbers.
 */
int add(int a, int b)
{
int n;

n = (a + b);
return (n);
}
