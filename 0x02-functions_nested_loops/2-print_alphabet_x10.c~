#include "main.h"
/**
 * main - Entry point
 *
 *Return: Always 0 (success)
 */
void print_alphabet_x10(void)
{
int ax10 = 0, a;

while (ax10 <= 10)
{
for (a = 'a' ; a <= 'z' ; a++)
{
_putchar(a);
}
ax10++;
_putchar('\n');
}
}
