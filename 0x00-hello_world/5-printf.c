#include <stdio.h>

/**
 * main - Entry point
 *
 * The function will output a string
 * Return: Always 0 (Success)
 */
int main(void)
{
char *m = "with proper grammar, but the outcome is a piece of art,";

printf("%s\n", m);

return (0);

}
